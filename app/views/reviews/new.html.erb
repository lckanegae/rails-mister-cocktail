<h1>Add a review</h1>

<%= simple_form_for [@cocktail, @review] do |f| %>
  <%= f.input :rating, collection: 0..5 %>
  <%= f.input :content %>
  <%= f.button :submit, "Criar review", class: "btn btn-primary btn-sm" %>
<% end %>
